---
title: "Podcasts"
date: "2025-02-01"
icon: "podcast"
---

import { Image } from "astro:assets";
import { podcasts } from "../../data/podcasts";

<div class="grid justify-center grid-cols-[repeat(auto-fit,minmax(12rem,1fr))]">
	{podcasts.map((podcast) => (
		<a
			href={podcast.url}
			target="_blank"
			class="flex flex-col p-8 outline-[0.5px] outline-muted gap-y-4 group"
		>
			<Image
				src={`/src/images/lists/podcasts/${podcast.cover}`}
				alt={podcast.title}
				width={300}
				height={300}
				class="object-cover rounded-xs aspect-square"
				loading="lazy"
				decoding="async"
				sizes="(max-width: 640px) 150px, 300px"
			/>
			<h3 class="text-sm font-medium">{podcast.title}</h3>
		</a>
	))}
</div>
