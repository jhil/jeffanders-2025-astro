---
title: "Podcasts"
date: "2025-02-01"
icon: "podcast"
---

import { getCollection } from "astro:content";
export const podcasts = await getCollection("podcasts");

<div class="grid justify-center grid-cols-[repeat(auto-fit,minmax(15rem,1fr))]">
	{podcasts.map((podcast) => {
		const fm = podcast;
		return (
			<a
				href={fm.url}
				target="_blank"
				class="outline outline-muted p-8 flex gap-y-4 group"
			>
				<div class="flex flex-col gap-y-1">
					<h3 class="text-sm font-medium group-hover:underline">{fm.title}</h3>
					<p class="text-xs text-gray-400 group-hover:text-gray-600">
						{fm.category}
					</p>
				</div>
			</a>
		);
	})}
</div>
