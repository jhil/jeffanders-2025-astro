---
title: "Angel"
date: "2025-02-01"
icon: "circle-multiple"
---

import { Image } from "astro:assets";
import { investments } from "../../data/investments";

<div class="grid justify-center grid-cols-[repeat(auto-fit,minmax(15rem,1fr))]">
	{investments.map((investment) => (
		<a
			href={investment.url}
			target="_blank"
			class="outline outline-muted p-8 flex gap-y-4 group center gap-4"
		>
			<Image
				src={
					investment.cover.includes("https")
						? investment.cover
						: `/src/images/lists/investments/${investment.cover}`
				}
				alt={investment.company}
				width={32}
				height={32}
				class="rounded-xs aspect-square object-contain"
				loading="lazy"
				decoding="async"
			/>
			<div class="flex flex-col justify-center">
				<h3 class="font-medium group-hover:underline">{investment.company}</h3>
				{investment.label && (
					<span class="text-sm text-gray-400">{investment.label}</span>
				)}
			</div>
		</a>
	))}
</div>
